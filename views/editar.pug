doctype html
head
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  title Login
  link(rel='stylesheet' href='/css/form.css')
body
.container
  .container-login
    form.login(method="post" action=`/editar/${imagen._id}` enctype="multipart/form-data")
      textarea(rows="2" name='descripcion' placeholder=`${imagen.descripcion}`)
      input.btn-login(type='submit' value='Login')